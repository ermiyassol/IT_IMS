const device = require("../routes/device.routes");
const employee = require("../routes/employee.routes");
const history = require("../routes/history.routes");


module.exports = {device, employee, history}